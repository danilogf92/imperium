<article class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <div class="flex items-center justify-between gap-3"><h3 class="font-bold text-slate-900">Alerts and data quality</h3><span @class(['rounded-full px-3 py-1 text-sm font-bold', 'bg-emerald-100 text-emerald-700' => $dataQuality['score'] >= 90, 'bg-amber-100 text-amber-700' => $dataQuality['score'] >= 70 && $dataQuality['score'] < 90, 'bg-red-100 text-red-700' => $dataQuality['score'] < 70])>{{ number_format($dataQuality['score'], 1) }}%</span></div>
    <div class="mt-4 space-y-2">@forelse ($executiveAlerts as $alert)<p @class(['rounded-lg border px-3 py-2 text-sm', 'border-red-200 bg-red-50 text-red-800' => $alert['level'] === 'danger', 'border-amber-200 bg-amber-50 text-amber-800' => $alert['level'] === 'warning'])>{{ $alert['text'] }}</p>@empty<p class="rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800">No critical alerts detected.</p>@endforelse</div>
    <div class="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-5">@foreach ($dataQuality['issues'] as $label => $count)<div class="rounded-lg bg-slate-50 p-2 text-center"><p class="text-lg font-bold text-slate-900">{{ $count }}</p><p class="truncate text-[10px] uppercase text-slate-500">Missing {{ $label }}</p></div>@endforeach</div>
</article>
